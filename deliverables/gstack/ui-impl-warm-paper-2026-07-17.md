# Quro AI UI 实现报告（按用户设计稿重写视觉）

**日期**：2026-07-17
**场景**：设计实现 / 全流程交付（设计稿 → 主题重写 → 界面重绘）
**参与成员**：设计顾问（设计系统映射，由主理人代执行）+ 主理人（实现与汇编）
**说明**：本会话 GStack 专职子 Agent（gstack-*）不可用，按降级策略由主理人直接完成「设计映射 + 代码实现 + 编译验证」。

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟢 通过（视觉已按两份设计稿各对齐各自草稿：聊天页=暖纸/陶土，设置页=灰底/靛蓝）
- 设计稿来源：聊天稿 `对话框设计稿ai-chat.html`（定义主视觉，原品牌「墨问」）→ 设置稿 `设置界面.html`（iOS 卡片结构）
- 关键决策：**每屏严格对齐各自草稿，不再强行统一**——聊天页用暖纸/陶土（聊天稿原样），设置页用 iOS 灰底+靛蓝（设置稿原样）；品牌保留 **Quro AI**（草稿里的「墨问」为设计占位词）
- 阻塞项数量：0（编译已通过）
- 新 APK：`C:/Users/admin/Desktop/QuroAI-debug.apk`（构建于 2026-07-17 06:04，`BUILD SUCCESSFUL`，仅 deprecation 警告无错误）
- 下一步：装新 APK，分别核对「聊天页=暖纸陶土」与「设置页=灰底靛蓝」是否各对齐其草稿

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go（视觉层完成，待真机验证） |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 1（决策：两稿配色冲突，改为各对齐各自草稿）/ 🟢 多项 |
| 关键行动项 | 3 条（装包验证两屏 / 可选接入 Fraunces 字体 / 可选对齐导航栏） |
| 建议负责人 | 主理人 / 用户验收 |

---

## 1. 各成员核心结论

### 🎨 设计师（设计系统与视觉）
- 核心判断：聊天稿（暖纸/陶土）与设置稿（iOS 灰底/靛蓝）本就是**两套不同配色**；按用户「一摸一样」要求，改为**每屏各自对齐其自己的草稿**，不再由我强行统一。
- 关键建议：主题 token 全部映射到 Material3 ColorScheme；气泡形状按草稿「助手左上小圆角 / 用户右上小圆角」；发送钮用陶土；思考块用鼠尾草绿 + 虚线边框观感。

### 🔧 主理人（实现与汇编）
- 核心判断：聊天屏结构已贴近草稿，最高杠杆是重写 `QuroTheme.kt` 一次性换肤；设置屏包进独立的 `QuroSettingsTheme`（iOS 灰底+靛蓝），与聊天页暖色主题互不干扰；思考卡/工具卡改暖色（仅作用于聊天页）。
- 关键建议：所有颜色走主题 token，硬编码的极光蓝/石板蓝已全部清除（仅 `CodeBlock` 保留深色代码卡，符合惯例）。

---

## 2. 设计 token → Compose 映射（核心交付物）

| 设计变量 | 值 | Compose token |
|------|------|------|
| `--paper` 主底 | `#F4F1EA` | `background` |
| `--paper-2` 次底 | `#ECE7DC` | `surfaceVariant` |
| `--card` 卡片 | `#FFFFFF` | `surface` |
| `--ink` 主文字 | `#211E1A` | `onBackground` / `onSurface` |
| `--ink-soft` 次文字 | `#544D44` | `onSurfaceVariant` |
| `--muted` 弱文字 | `#938A7E` | （占位用 `onSurfaceVariant`） |
| `--line` / `--line-2` 边框 | `#E3DDD0` / `#D8D0C0` | `outline` / `outlineVariant` |
| `--accent` 陶土 | `#C25A38` | `primary` |
| `--accent-press` | `#A8482B` | `onPrimaryContainer` |
| `--accent-soft` | `#F4E4DB` | `primaryContainer`（用户气泡底） |
| `--sage` 鼠尾草绿 | `#6E7C62` | `secondary`（思考/工具标题） |
| `--gold` 点缀金 | `#B8902F` | `tertiary` |
| 品牌衬线 | Fraunces / Noto Serif SC | `FontFamily.Serif`（近似，真字体待接入） |
| 背景微光 | 顶部 `#FBF9F4` → 纸 → 次纸 | `QuroPaperBrushLight` 纵向渐变 |

**暗色方案**：暖近黑底（`#16130F` 系）+ 浅陶土强调（`#D9785A`），保证暗色下对比度。

---

## 3. 代码改动清单

| 文件 | 改动 |
|------|------|
| `ui/theme/QuroTheme.kt` | 整文件重写：删除极光渐变 + frosted，改为暖纸/陶土 ColorScheme（明+暗）+ 纸感背景画刷；新增 `QuroSettingsTheme`（iOS 灰底 #f2f2f7 + 靛蓝 #5856d6）供设置页独立套用 |
| `ui/QuroChatScreen.kt` | 顶栏底色改为纸感半透；输入条改为白卡片圆角 20 + 边框；占位符改 `onSurfaceVariant`；气泡：用户=陶土浅底/助手=白卡 + 草稿圆角 + 边框；工具卡/工具调用文字改暖色；默认头像文字改 `onPrimary` |
| `ui/QuroRichContent.kt` | `ThinkingCard` 改暖色（surfaceVariant + 边框 + 鼠尾草绿标题），加 `BorderStroke` 导入 |
| `ui/QuroSettingsScreen.kt` | 整文件重写并包进 `QuroSettingsTheme`（iOS 灰底 #f2f2f7 + 靛蓝 #5856d6）：大标题（无衬线，对齐草稿）+ 白圆角卡片(半径12)（模型配置 / 功能列表）+ 靛蓝图标方块 + 底部本地存储说明 + 「更多功能即将推出」占位 |

> 所有改动仅触及颜色/形状/排版，**未改动任何既有逻辑**（实时配置发送、崩溃自报告、附件、命令面板、思考开关、人格卡均保留）。

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 安装新 APK，分别核对「聊天页=暖纸陶土」与「设置页=灰底靛蓝」是否各对齐其草稿 | 用户 | P0 | 装包后 |
| 2 | 可选：接入 Fraunces + Noto Serif SC 字体，让聊天页品牌衬线更贴草稿（当前系统 `FontFamily.Serif` 近似） | 主理人 | P3 | 后续迭代 |
| 3 | 可选：底部导航栏目前为全局暖色；如需设置页下也呈 iOS 灰，可逐屏重主题导航栏 | 主理人 | P3 | 后续迭代 |

---

## ⚠️ 待完善 / 已知局限

- **两屏刻意不同配色**：聊天页=暖纸/陶土（聊天稿），设置页=iOS 灰底/靛蓝（设置稿）—— 因两份草稿本就不同；按用户「一摸一样」对齐各自草稿的取舍，非遗漏。
- **品牌字体**：Compose 用系统 `FontFamily.Serif` 近似，未内置 Fraunces/Noto Serif SC（需字体资源文件，非阻塞）。
- **GStack 子 Agent 不可用**：本会话 `gstack-*` 专家无法调度，由主理人直接执行；专业产出由主理人依据设计稿转述，关键决策仍建议工程负责人复核。
- **真机验证缺失**：沙箱无设备/adb，无法实机截图核对，仅保证编译通过与代码级视觉对齐。

---

## 📚 成员产出索引

- 设计映射与实现均由主理人执行（子 Agent 不可用），无独立成员原始产出文件。设计稿原文：
  - `C:/Users/admin/Downloads/对话框设计稿ai-chat.html`
  - `C:/Users/admin/Downloads/设置界面.html`

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
