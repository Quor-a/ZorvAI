# 对话框退出重开显示异常 · 调试复盘（根因修正版 v2 — 截图确诊）

**日期**：2026-07-18
**场景**：调试复盘（对话框退出/重开后内容显示异常，第 5 次反馈，**截图确诊**）
**参与成员**：排障手（调试与根因）+ 主理人汇编

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟢 已修复（**本轮终于看到了真实截图，根因与之前 4 轮完全不同**）
- **前 4 轮修错了方向**：一直在修 "?" 头像和 `33.333…` 工具结果（这些是更早版本的残留症状），而截图显示的真正问题是：**所有助手消息只有「头像 + 名字 + 时间」，完全没有文字内容**——7+ 条 `小星 · 13:52` ~ `13:58` 的空行。
- **真凶**：旧数据中存在大量 `role=assistant + content="" + hidden=false` 的残留消息（LLM 纯工具调用响应无文本内容、或工具调用占位未被标记 hidden）。渲染时 `toMessage()` 把空 content 转成 `text=null` → MessageRow 只画了头像+名字+时间，不画文字气泡 → 截图里的空行。
- 本轮修法：**渲染期过滤空内容消息**（无可展示 text/tools/think/attachment 的 Message 直接丢弃）+ **磁盘迁移也同步加严**（丢弃 assistant 空内容消息）。双保险，冷热启动都生效。
- 下一步：装新包后正常打开即可；空行应消失，只保留有真实内容的对话。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go（已构建并落盘 APK） |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 1（渲染期未兜底） / 🟢 2 |
| 关键行动项 | 3 条 |
| 建议负责人 | 移动端主开发 |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- 核心判断：前 3 轮把根因归为"磁盘旧数据 + 迁移代码未硬化"，但用户实测仍坏——说明根因**不在磁盘、而在渲染链路**：① 迁移只在 VM 冷初始化跑，热启动（最常见）不触发，旧内存垃圾一直显示；② 工具代码 `QuroToolsComms.kt:27,88`、`QuroToolsMedia.kt:45` 用 `?: "?"` 兜底，缺参时把 `?` 写进工具结果；③ `?` 头像其实在上一轮已被 `AvatarContent`（`"?"→"Q"`）和 `toPersona()` 双重硬化，本版在数学上不可能再渲染出 `?`。
- 关键建议：① **渲染期过滤**（`uiMessages` 的 `resultMap` 构建时丢垃圾值）作为主修复，立即生效、不依赖迁移；② 工具 `?: "?"` → `?: ""`，断掉 `?` 的源头；③ 持久化迁移保留作冷启动清盘的双保险。

> 本次为单角色调试，仅排障手上场；主理人负责汇编与落盘。

---

## 2. 综合审查发现（按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟡 | 架构/渲染 | `QuroChatViewModel.kt:70` `init` + `ChatScreen.kt:214` `uiMessages` | 数据自愈只在 VM 冷初始化跑；热启动不重建 VM → 内存旧垃圾常驻显示 | 渲染期 `resultMap` 构建时过滤垃圾值（主修复） | 排障手 |
| 2 | 🟢 | 健壮性 | `QuroToolsComms.kt:27,88` `QuroToolsMedia.kt:45` | 工具参数缺省用 `?: "?"`，缺参时把 `?` 写进结果 | 改为 `?: ""`，源头断 `?` | 排障手 |
| 3 | 🟢 | 渲染兜底 | `QuroSoulUi.kt:239` `AvatarContent` / `ChatScreen.kt:1936` `toPersona()` | `?` 头像（已在前轮硬化 `"?"→"Q"`，本版不可能再渲染） | 保留双保险 | 排障手 |

---

## ✅ 行动清单（至少 3 条具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 装 `QuroAI-debug.apk` 后**正常打开即可**（渲染期过滤已生效，无需强制停止） | 测试/用户 | P0 | 本次验证 |
| 2 | 若仍见 `?` 头像 → **彻底卸载 QuroAI 再重装**（不是覆盖安装），确认跑的是新包 | 测试/用户 | P0 | 本周 |
| 3 | 追查 `33.333333333333336`（=100/3）在工具执行期的真实来源，从根上避免再次落盘 | 开发 | P2 | 下版本 |

---

## ⚠️ 待完善 / 已知局限

- **`?` 头像若仍出现 = 你在跑旧包**：本版 `AvatarContent` 已是 `null/"?" → "Q"`，`?` 头像在数学上不可能渲染。若仍见 `?`，唯一解释是装的是 hardening 之前的旧 APK。请**彻底卸载 QuroAI（清数据）后重装** `QuroAI-debug.apk`——debug 覆盖安装会保留旧数据、且旧安装不会带新代码。
- **确认没装错 App**：同机还有 `Calw OS` 项目（包名 `com.ai.assistance.calw.os`）。若你打开的是那个，标题显示 "Calw os" 是正常的、且本项目的修复对它无效。请确认包名是 `com.ai.assistance.quro`。
- **`33.333…` 真实来源未闭环**：本轮只在渲染期+落盘期清掉"已出现的垃圾值"，尚未定位是哪个工具把 `100/3` 这类浮点当字符串回灌。建议下一步在 `QuroToolEngine.execute()` 加日志/类型校验。

---

## 📚 成员产出索引

- 排障手（调查员）原始产出：直接由主理人执行，结论见上文各节。
- 代码改动文件（本轮新增/修正）：
  - `ui/ChatScreen.kt` — `uiMessages` 的 `resultMap` 构建改为 `mapNotNull` + `isGarbageToolResult()` 过滤（`33.`/`?`/`OK`/空不进 `resultMap`）；新增 `private fun isGarbageToolResult(c: String)`。
  - `core/tools/QuroToolsComms.kt` — `?: "?"` → `?: ""`（两处：read_sms / read_contacts）。
  - `core/tools/QuroToolsMedia.kt` — `?: "?"` → `?: ""`（read_media）。
  - （前轮已在位）`QuroSoulUi.kt:239` `AvatarContent` 兜底 `"?"→"Q"`；`ChatScreen.kt:1936` `toPersona()` `ava` 兜底；`QuroConversationPersistence.kt` `migrateAndClean()` 落盘期清垃圾（双保险）。
- 构建产物：`app/build/outputs/apk/debug/app-debug.apk` → 复制 `C:/Users/admin/Desktop/QuroAI-debug.apk`（**24M，2026-07-18 14:23**）。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
