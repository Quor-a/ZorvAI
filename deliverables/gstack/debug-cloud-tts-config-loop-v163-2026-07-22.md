# 语音导航根因纠正：云模型配置 ≠ 语音服务 Hub（v163）

**日期**：2026-07-22
**场景**：调试复盘（导航语义错误 + 死循环根因纠正）
**参与成员**：排障手（gstack-investigator）

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟢 修复（循环根因纠正）
- 阻塞项数量：0
- 用户原话指正："是你搞错了，前往语音服务设置是语音合成云模型配置你关联到你关联到语音服务又回来了"
- 核心纠正：TTS 页的"前往语音服务设置"按钮本质是**语音合成云模型配置**入口（选择/配置云 TTS 服务商），此前被误关联到**语音服务 Hub**，造成 TTS↔Hub 死循环。
- 下一步：装 v163 复测独立云模型配置屏可达、无循环。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go（循环已断开） |
| 严重度分布 | 🔴 0 / 🟠 1（导航语义错误导致的死循环）/ 🟡 0 / 🟢 0 |
| 关键行动项 | 2 条（重建屏 + 接线） |
| 建议负责人 | 排障手 |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- 核心判断：v152~v162 把 TTS 页"云模型配置"入口错误接到语音服务 Hub（`showVoiceService`），而 Hub 的"语音合成(TTS)"卡片又回到 TTS 页，形成必然死循环。根本症结是 `QuroCloudTtsConfigScreen` 被错误定义为 `= QuroVoiceServiceScreen`（Hub 别名），使"云模型配置"与"语音服务 Hub"被等同。
- 关键建议：将两者解耦——重建真正的"云模型配置屏"（列出 13 个云 TTS 服务商 + 动态配置表单），TTS 页入口指向它；保留 Hub 仅作 TTS/STT/语音设置三者的总览，从设置弹层进入。

> 本次仅排障手上场。

---

## 2. 综合审查发现

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟠 | 导航/语义 | QuroCloudTtsConfigScreen.kt:11 / ChatScreen.kt TTS 面板 onOpenCloudConfig | "云模型配置"被错误等同为"语音服务 Hub"，TTS 页入口接 `showVoiceService` 导致 TTS↔Hub 死循环 | 重建独立云模型配置屏并重新接线 | 排障手 |

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 重写 `QuroCloudTtsConfigScreen` 为真正的云模型配置屏（服务商列表 + 动态配置 + 保存） | 排障手 | P0 | v163 已完成 |
| 2 | ChatScreen 新增 `showCloudTts`、TTS 面板 `onOpenCloudConfig` 指向它、渲染独立面板、加 `ui_open_cloud_tts` | 排障手 | P0 | v163 已完成 |
| 3 | TTS 页 SOURCE_CLOUD 分支恢复"配置云模型服务商"按钮 | 排障手 | P0 | v163 已完成 |

---

## ⚠️ 待完善 / 已知局限

- 云模型配置屏目前仅覆盖"服务商选择 + 字段配置 + 保存 + 已配置状态"；音色/风格标签/格式选择未纳入（数据模型 `QuroTtsProviderConfig` 已支持，后续可按需扩展 UI）。
- TTS 页顶部 `OutlinedButton`、云模型行末 `ChevronRight` 两个旧入口在 v162 已删、本次未恢复（仅在 SOURCE_CLOUD 分支恢复最贴合语义的"配置云模型服务商"按钮）。如用户希望其它位置也出现入口，再行补充。
- 诊断 Toast（DBG·*）仍保留在 Hub 面板与卡片回调中，便于后续排障；如需清理可一并移除。

---

## 📚 成员产出索引

- gstack-investigator（排障手）原始产出：见本文件；代码改动见 v163 提交（QuroCloudTtsConfigScreen.kt 重写、ChatScreen.kt 加 showCloudTts、QuroTtsSettingsScreen.kt 恢复按钮、versionCode 162→163）。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
