# 设置子屏返回导航回归排查 + STT 模型加载修复复盘

**日期**：2026-07-19
**场景**：调试复盘（导航回归）+ 功能交付（v57）
**参与成员**：排障手（gstack-investigator）· 主理人（编译验证 + 收口）

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟢 通过（导航回归已修复并编译通过，v57 已出包）
- 阻塞项数量：0
- v54「设置→子屏→返回上一层」方向正确，但子屏 `zIndex` 为 0、被设置弹层（zIndex 50）盖住，且弹层自身无 `BackHandler`，导致「子屏看不见 / 返回没反应」。本次补两处修复。
- STT 本地模型在 21:06 仍加载失败的日志来自 **v56 之前的旧包**；v56 已加入「模型体积 < 1MB 直接拒绝 + 清部署记录」的兜底，v57 在其基础上叠加导航修复。
- v57 APK：`QuroAI-debug-2026-07-19-v57.apk`（桌面）。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go（导航回归已修，编译通过） |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 1（导航层级缺陷）/ 🟢 0 |
| 关键行动项 | 3 条 |
| 建议负责人 | 主理人 / 排障手 |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- 核心判断：v54 移除 `onOpenX` 里的 `sheet = null` **是对的**（设置弹层本应留在子屏后面）；真正缺陷是 (1) 子屏 `Box` 默认 `zIndex 0`，被 `SheetOverlay` 的 `zIndex(50f)` 遮在下面，子屏不可见/不可点；(2) `SheetOverlay` 自身没有 `BackHandler`，仅靠遮罩 `clickable` 关弹层，导致「设置打开时按返回键关不掉、像没反应」。
- 关键建议：Fix A 给所有全屏子屏 `Box` 加 `.zIndex(100f)`；Fix B 给 `SheetOverlay` 加 `BackHandler(enabled = sheet != null) { onDismiss() }`。Compose 中后注册的 `BackHandler` 优先级更高，子屏 `BackHandler`（注册在后）先于弹层 `BackHandler`（注册在前）触发，链路正好为「子屏→设置→聊天」。

### 🧑‍💼 主理人（编译验证 + 收口）
- 核心判断：已逐一回读核实两处改动落盘正确；`env -i` 绕过会话超大环境变量（~396KB > MSYS 32KB 上限）后 `clean assembleDebug` 编译通过，无错误（仅历史 deprecation 警告）。
- 关键建议：v57 已出包，用户在真机安装后按「设置→模型配置→返回→再返回」验证三层回退。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟡 | UI/导航 | ChatScreen.kt:1508 + 655/663/671/679/732/740/748/756/765/773/783 | 子屏 `Box` 默认 zIndex 0，被 `SheetOverlay`(zIndex 50) 覆盖，且弹层无 `BackHandler`，导致子屏不可见 + 返回键关不掉设置 | Fix A：`zIndex(100f)`；Fix B：弹层加 `BackHandler` | 排障手 |

---

## ✅ 行动清单（至少 3 条具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 安装 `QuroAI-debug-2026-07-19-v57.apk`，验证「设置→模型配置→返回→再返回」三层回退 | 用户/QA | P0 | 立即 |
| 2 | 若 STT 仍失败（v57 首启会自动清 3452 字节假模型），进 STT 设置删部署模型→重下；来源被墙则切 `paraformer-zh`/`whisper-tiny` | 用户 | P1 | 本周 |
| 3 | 历史 `Icons.Filled.ArrowBack` deprecation 警告（非阻塞）后续统一换 `AutoMirrored` 版本 | 主理人 | P3 | 后续版本 |

---

## ⚠️ 待完善 / 已知局限

- 21:06 的 STT 失败日志跑在 v56 旧包上；v56/v57 的「体积兜底 + 清部署」尚未在用户真机验证。
- `browserUrl` 内联 Box（783）已加 zIndex(100f)，但其本身无 `BackHandler`：若由设置内入口（工具箱→打开浏览器）触发且设置弹层仍开，返回会先关设置弹层、浏览器留在前台——属边缘情况，本次未额外加浏览器 `BackHandler`（2419 处的 Dialog 浏览器自带 `BackHandler`，不受影响）。
- 编译环境：会话注入的环境变量约 396KB，超过 MSYS exec 上限，必须用 `cd 项目目录 && env -i <最小变量> ./gradlew --no-daemon` 才能编译。

---

## 📚 成员产出索引

- gstack-investigator（排障手）原始产出：根因分析 + 11 处 `zIndex(100f)` 改写 + `SheetOverlay` BackHandler 插入，已逐行回读确认，未跑 Gradle。
- 主理人收口：核对改动（1505-1507、765 等）、`app/build.gradle.kts` versionCode 56→57、执行 `env -i` 编译（`BUILD SUCCESSFUL` 1m40s）、出包至桌面、撰写本报告。

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
