# v151 出包 + v149/v150 不可见根因调查报告

**日期**：2026-07-22
**场景**：调试复盘（根因调查）+ QA 发布验证（Release Gate）
**参与成员**：排障手（gstack-investigator）+ 质量门神（gstack-qa-lead）

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟡 条件 Go（APK 完整可信，但 v149/v150"不可见"尚未真机确认，须以 clean reinstall 真机验证为放行门槛）
- 阻塞项数量：2（未真机确认 v149/v150；C 盘余量仅 1.8GB 偏紧）
- 根因结论：v149（组件合体气泡）与 v150（元宝链接卡片）**代码均已完整实现并正确接线**，用户看到"旧款/没实现"是**陈旧/损坏安装包**导致，非代码缺陷。
- 下一步：用户卸载旧包 → 装 v151 → 按触发条件复测（发 yb.tencent.com 链接看元宝卡；让 AI 下发组件卡看合体）。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟡 条件 Go（clean reinstall 真机验证通过后即 Go） |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 1（陈旧安装，非缺陷）/ 🟢 全部源码接线到位 |
| 关键行动项 | 3 条（clean reinstall、按触发条件复测、盯 C 盘余量） |
| 建议负责人 | 用户（真机验证）；主理人（如需进一步排查则文字描述现象） |

---

## 1. 各成员核心结论（每位 1 段）

### 🔧 排障手（根因调查）
独立审计 ChatScreen.kt / QuroChatCard.kt / QuroChatCards.kt 源码，确认 v149 气泡合体渲染（`msg.cards + inlineCards` 后 `QuroChatCardView` 统一走单一 MessageRow，无独立底栏）与 v150 元宝卡片（`YuanbaoCard` 定义 + `https?://(?:yb|yuanbao)\.tencent\.com/\S+` 正则 + `YuanbaoCardView` 派发/渲染/点击内置浏览器）均已完整接线，`extractInlineComponents` 支持 36+ 组件类型。构建产物 194,900,897 字节、与桌面副本一致。结论：**非代码缺陷，是陈旧/损坏安装包**；严重度 🟢；仅边缘提示纯卡片无文本消息不走该渲染分支（线上均随文本下发，不影响现状）。

### ✅ 质量门神（QA测试与发布）
核验 APK 完整（cmp 一致）、versionCode=151、C 盘余 1.8GB（≥500MB 达标但偏紧）、旧包已备份 `D:\QuroAI_old_apks_backup\`、v151 新功能源码齐全。决策 🟡 条件 Go：阻塞项为 v149/v150 未真机确认、C 盘余量偏紧。给出 clean reinstall 验证清单（卸载→装 v151→验证 5 项：后台自启 tab / 元宝卡 / 组件合体 / 系统分享 / 开机自启）、逐条 pass/fail 判据、回滚预案（异常即卸回备份 v150）、已知风险（C 盘空间、厂商 ROM 自启限制、组件非常驻）。

> 未上场成员不列。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟡 | 安装/发布 | 用户设备 | v149/v150 在真机"不可见"，根因是此前长期构建失败的 v151 包（State 类型错误）导致用户装的可能是陈旧/损坏旧包 | clean reinstall v151 后按触发条件复测 | 排障手 + 质量门神 |
| 2 | 🟡 | 环境 | C 盘 | C 盘余量仅 1.8GB，安装/解包期间若跌破 500MB 有截断风险（历史曾撑满致"安装包损坏"） | 装前确认 ≥500MB；不足先删桌面 v151 副本/构建缓存 | 质量门神 |
| 3 | 🟢 | 代码 | ChatScreen.kt:1437 等 | v149/v150 接线经审计全部到位，无阻断性缺陷 | 无需改码，验证即可 | 排障手 |
| 4 | 🟢 | 边缘 | ChatScreen.kt `if(!msg.text.isNullOrBlank())` 块 | 纯卡片无文本消息不走该渲染分支（线上卡片均随文本下发，不影响现状） | 可选：放宽分支以支持纯卡片消息（非阻塞） | 排障手 |

---

## ✅ 行动清单（至少 3 条具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 卸载旧包 `adb uninstall com.ai.assistance.quro`，装 `QuroAI-debug-2026-07-22-v151.apk`（确认无截断报错） | 用户 | P0 | 立即 |
| 2 | 按触发条件复测：① 发 `https://yb.tencent.com/...` 链接看元宝卡 ② 让 AI 用组件工具输出卡片看合体进气泡 ③ 设置页出现"后台自启动"tab | 用户 | P0 | 复测当日 |
| 3 | 装前确认 C 盘 ≥500MB 余量；不足先清理再装 | 用户 | P1 | 装前 |
| 4 | 若仍异常，文字描述具体现象（主理人看不到截图）以便定位 | 用户 | P1 | 复测后 |

---

## ⚠️ 待完善 / 已知局限

- **截图问题未定位**：用户提到"截图上的问题也没有解决"，但主理人/成员均无法查看截图（返回 Content filtered）。需用户用文字描述具体 UI 现象才能排查。
- **厂商 ROM 自启限制**：开机自启（v151 新功能）在部分 ROM 可能被限制，属系统侧限制而非 App 缺陷，记为已知局限。
- **组件非常驻**：v149 富组件 / v150 元宝卡均为"AI 下发才出现"，验证须走真实对话/链接，非凭空常驻。
- **纯卡片无文本分支**：ChatScreen.kt 渲染位于 `if(!msg.text.isNullOrBlank())` 内，纯卡片消息不走该路径——当前不影响，后续可放宽。

---

## 📚 成员产出索引

- gstack-investigator（排障手）原始产出：源码审计结论（ChatScreen.kt:1437/1440、:3172、QuroChatCard.kt:373/389、QuroChatCards.kt:188/1216-1234；APK 194,900,897 字节一致；严重度 🟢；建议 clean reinstall 复测）
- gstack-qa-lead（质量门神）原始产出：v151 Release Gate（🟡 条件 Go；clean reinstall 5 项验证清单；pass/fail 判据；回滚至 D:\QuroAI_old_apks_backup\QuroAI-debug-2026-07-22-v150.apk；已知风险）

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
