# 语音服务「进不去」全树排查复盘（v159）

**日期**：2026-07-22
**场景**：调试复盘 / 导航链路审查
**参与成员**：排障手（gstack-investigator）

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟡 有条件通过（代码链路正确，已修一处真实视觉缺陷；待用户复测确认）
- 用户装 v158 仍报「还是进不去」。本次对**整条语音导航树**做了全量排查。
- 结论一：ChatScreen 里 TTS→Hub、设置弹层→Hub、Hub→子面板 的**代码接线完全正确**，不存在死循环或无效回调。
- 结论二：发现一处真实缺陷——TTS/STT/语音服务三个底部面板**漏了背景色**，导致底层设置弹层透出、与面板重叠，视觉上极易误判为「没进去 / 点不动」。
- 已修复（v159）：三个面板补 `.background(cs.background)`，与既有全屏面板写法对齐。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟡 条件 Go（待用户确认 1.0.159 复测） |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 1（透明面板视觉歧义）/ 🟢 0 |
| 关键行动项 | 3 条 |
| 建议负责人 | 排障手 / 用户复测 |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- 核心判断：语音导航**代码层面全通**（v158 已把死循环彻底移除，本次复读确认无残留）。真正的可观测缺陷是三个底部面板缺少不透明背景，使设置弹层透过透明区域叠在面板之上，造成「进不去」的视觉错觉。
- 关键建议：补背景色彻底盖住设置弹层；请用户确认安装版本为 1.0.159 并描述点击后的具体现象，以区分「视觉歧义」与「真导航失效」。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟡 | UI/层级 | ChatScreen.kt TTS/STT/VoiceService 面板 Box（原 953/967/981） | 底部面板用 `Box(Modifier.fillMaxSize().zIndex(100f))` 无 `.background(...)`，设置弹层（zIndex 50）透过透明区域叠出，视觉与交互歧义 | 补 `.background(cs.background)`（已修 v159） | 排障手 |

### 全树接线核对（均正确，列此排除嫌疑）
- TTS 页三入口 `onClick = { onOpenCloudConfig() }` → ChatScreen `onOpenCloudConfig = { showTts=false; showVoiceService=true }`（line 959）✓
- 设置弹层「语音服务」行（line 2464）→ `onOpenVoiceService → showVoiceService=true` ✓
- 上传弹层「语音服务」块（line 2737）→ 同上 ✓
- Hub 三卡片（line 987）→ `onOpenTts→showTts` / `onOpenStt→showStt` / `onOpenVoiceSettings→showVoice` ✓
- 层级：语音面板 `zIndex 100/101` > 设置弹层 `zIndex 50`（SheetOverlay 整体在 `zIndex(50f)` Box 内，line 2349）✓
- `QuroVoiceServiceScreen` 渲染三卡片正常、无外部依赖、不在 scroll 中（无无限约束风险）✓

---

## ✅ 行动清单（至少 3 条具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 卸载旧包 → 装 v159 → 「应用信息」确认版本为 **1.0.159** | 用户 | P0 | 立即 |
| 2 | 进 TTS 设置页点「前往语音服务设置 ›」→ 确认出现**不透明、盖满下方**的语音服务 Hub（含三卡片） | 用户 | P0 | 立即 |
| 3 | 若仍异常，描述现象：点按钮后屏幕有变化吗？Hub 出现但卡片点不动？还是完全无反应？——据此区分视觉 vs 真导航失效 | 用户 | P1 | 复测时 |

---

## ⚠️ 待完善 / 已知局限

- 本次修复基于静态代码审计 + 构建验证，**未运行真机/模拟器截图确认**（环境无可用 emulator）。若 v159 复测仍「进不去」，需用上述第 3 条现象描述定位，可能涉及运行期状态（如 `showVoiceService` 被某 `LaunchedEffect` 意外重置，当前审计未见此类代码）。
- 无关 warning：`Icons.Filled.ArrowBack` → 建议改用 `Icons.AutoMirrored.Filled.ArrowBack`（Material 已 deprecate，非错误，可后续统一清理）。

---

## 📚 成员产出索引

- gstack-investigator（排障手）原始产出：本次排查覆盖 ChatScreen.kt 语音导航全链路（line 387-391 / 719-834 / 948-990 / 2289-2411 / 2429-2470 / 2697-2740 / 2953-2972）+ QuroTtsSettingsScreen.kt（line 49/100-122/189/436）+ QuroVoiceServiceScreen.kt（全文）。结论：接线正确，缺陷为透明面板漏背景色，已修 v159。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
