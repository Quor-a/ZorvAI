# 工具调用结果被截断（"还是不完整"）修复报告

**日期**：2026-07-18
**场景**：调试复盘（工具结果展示截断）
**参与成员**：排障手（调查员）

---

## 📌 TL;DR（执行摘要）

- 用户本轮给出的 15 次工具调用日志**证明上一轮「43 工具 + 菜单注入」修复已生效**：模型现在知道有什么工具、也会正确选用（设备管理 / 应用启动 / 屏幕操控 / 文件读写全覆盖）。
- 用户标注的「还是不完整」落在 `read_text_file` 读回的 HTML：根因是 **工具结果气泡被 `maxLines = 8` 硬截断**，上百行 HTML 只露前 8 行。
- 本次修复：去掉截断，改为**可滚动、限高（结果 240dp / 参数 160dp）的等宽文本框**，完整内容可见。
- 编译通过（BUILD SUCCESSFUL，仅既有 ClickableText deprecation 警告）。
- 关于「不能调用多个工具」：代码端**已完整支持**一条回复并行多个 tool_calls（引擎 `engine.execute(ctx, callsWithId)` 一次性执行全部），模型（MiMo）当前选择每次只发一个，属模型行为，非代码限制。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go |
| 严重度分布 | 🔴 0 / 🟠 1（结果截断）/ 🟡 0 / 🟢 0 |
| 关键行动项 | 1 条（已修复） |
| 建议负责人 | 排障手 |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- 核心判断：工具调用能力本身已修复（用户日志为证）；剩余「不完整」是 UI 展示层对工具结果的 8 行截断，与引擎/工具定义无关。
- 关键建议：结果/参数均改为可滚动等宽文本框；并行多工具属模型侧行为，已在系统提示词强化并行示例引导。

---

## 2. 综合审查发现

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟠 | UI 截断 | `ChatScreen.kt` `ToolCallBlock` L1140 | 工具结果 `Text` 设 `maxLines = 8`，长结果（如读回的 HTML 源码）被截断，用户体感"不完整" | 改为 `verticalScroll` + `heightIn(max=240.dp)` 等宽文本框 | 排障手 |
| 2 | 🟢 | UI 截断 | `ChatScreen.kt` `ToolCallBlock` L1133 | 工具参数同样 `maxLines = 6`，长参数（如 `write_file` 的 content）也会截断 | 同改为可滚动等宽文本框（限高 160dp） | 排障手 |

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 装新包后验证 `read_text_file` 读回的 HTML 是否完整显示（可滚动查看全文） | 用户 | P0 | 本轮 |
| 2 | 若仍希望模型「一条回复并行多工具」，可在系统提示词进一步加硬性规则（当前已含并行示例）；属模型行为调优，非代码缺陷 | 排障手 | P2 | 视反馈 |

---

## ⚠️ 待完善 / 已知局限

- 工具结果目前按**纯文本/源码**展示（等宽），未尝试把结果里的 HTML 渲染成富文本——因为 `read_text_file` 场景用户要"看完整代码源码"，渲染成网页反而看不清标签。若后续有"返回格式化 HTML 用于展示"的工具，再单独加渲染分支。
- 嵌套滚动：`verticalScroll` 位于 `LazyColumn` 消息项内，限高容器内滚动正常；极长结果（>240dp）需手动滚动查看，符合预期。

---

## 📚 成员产出索引

- 排障手（调查员）原始产出：本次直接定位 `ToolCallBlock` 两处 `maxLines` 截断并修复，编译验证通过。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
