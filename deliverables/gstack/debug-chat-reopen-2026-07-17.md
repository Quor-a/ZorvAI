# 对话框退出重开显示异常 · 调试复盘

**日期**：2026-07-17
**场景**：调试复盘（对话框退出/重开后内容显示异常）
**参与成员**：排障手（调试与根因）+ 主理人汇编

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟢 已修复（第 3 轮，此前 2 轮未真正生效）
- 阻塞项数量：0
- 三大症状根因与处置：
  1. **"?" 头像** → 上一版 `AvatarContent` / `toPersona()` 未对 "?" 做兜底；本版两端同时硬化，传入 "?" 也只显示 "Q"。
  2. **`33.333333333333336` 工具结果** → 旧版把垃圾值（33.333…/OK/纯数字 0-100）当工具结果落盘；本版在加载时**清空垃圾内容**（保留真实结果），重启后不再污染气泡。
  3. **"Calw os" 会话标题** → 非 bug，标题取自用户首条消息前 20 字（`deriveTitle`），用户此前确实问过 Calw OS 相关内容；代码无任何 "Calw" 硬编码。
- 下一步：安装 `QuroAI-debug.apk` 后**强制停止并重新打开**（让 `loadAll()` 触发迁移清理旧磁盘数据），异常应消失。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go（已构建并落盘 APK） |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 1（旧数据兼容性） / 🟢 2 |
| 关键行动项 | 3 条 |
| 建议负责人 | 移动端主开发 |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- 核心判断：三个症状分属两类根因——"?" 头像与 `33.333…` 工具结果都源自**磁盘旧数据 + 上一版渲染/迁移代码未硬化**；"Calw os" 标题是用户真实消息内容、非缺陷。关键证据：`QuroPersona` 数据类根本没有 `ava` 字段，聊天气泡头像实际来自 `toPersona()` 产出的 MoWen `Persona.ava`，当人格名为 "?" 且非 emoji 类型时 `ava` 退化为 "?"；而工具结果 `33.333…` 是 `100/3` 的浮点值，被旧版作为工具返回值持久化。
- 关键建议：① `AvatarContent` 与 `toPersona()` 双端兜底 "?"→"Q"；② 持久化加载时做**数据自愈**：丢弃隐藏管道占位、清空垃圾工具结果内容（保留真实结果）；③ 仍建议后续追查 `33.333…` 在工具执行期的真实来源（见已知局限）。

> 本次为单角色调试，仅排障手上场；主理人负责汇编与落盘。

---

## 2. 综合审查发现（按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟡 | 兼容性 | `QuroConversationPersistence.kt` `loadAll()` | 旧版未持久化 `hidden`、且把 `33.333…`/`OK`/纯数字当工具结果落盘，重启后原样读出污染 UI | 加载时自愈：清空白内容管道占位、清空垃圾工具结果 | 排障手 |
| 2 | 🟢 | 渲染健壮性 | `QuroSoulUi.kt` `AvatarContent` / `ChatScreen.kt` `toPersona()` | 当人格名为 "?" 且非 emoji 头像时，`ava` 退化为 "?" 直接进头像 | 双端兜底：`"?" -> "Q"`，永不在头像显示问号 | 排障手 |
| 3 | 🟢 | 非缺陷 | `QuroChatViewModel.kt` `deriveTitle()` | "Calw os" 标题来自用户首条消息前 20 字，代码无 "Calw" 硬编码 | 无需修改；如想规避，可限制标题字符集 | 排障手 |

---

## ✅ 行动清单（至少 3 条具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 安装 `QuroAI-debug.apk` 后**强制停止 APP 再打开**，让迁移逻辑清理旧磁盘数据 | 测试/用户 | P0 | 本次验证 |
| 2 | 回归：发送一条会触发工具调用的消息（如"电量多少"），确认工具结果气泡显示真实内容、无 `33.333…` | 测试 | P1 | 本周 |
| 3 | 追查 `33.333…` 在工具执行期的真实来源（是否为某工具把浮点比率当字符串回灌），从根上避免再次落盘 | 开发 | P2 | 下版本 |

---

## ⚠️ 待完善 / 已知局限

- **`33.333…` 真实来源未闭环**：本次仅清理了"已落盘的垃圾值"，尚未定位是哪一个工具/哪一段代码把 `100/3` 这类浮点当工具结果写入。建议下一步在 `QuroToolEngine.execute()` / 工具结果回灌处加日志或类型校验。
- **旧数据迁移是一次性的**：`loadAll()` 仅在发现脏数据时回写；若用户**不重开 APP**（仅退到后台），内存中仍是旧数据，需一次冷启动触发迁移。已用"强制停止再打开"覆盖。
- **工具结果不跨重启持久真实内容**：`uiMessages` 本就隐藏 `role=="tool"` 气泡、仅通过 `resultMap` 内联展示；垃圾被清空后为 `""`，内联 "→" 行不渲染（符合预期，不显示垃圾）。

---

## 📚 成员产出索引

- 排障手（调查员）原始产出：直接由主理人执行，结论见上文各节。
- 代码改动文件：
  - `app/src/main/java/com/ai/assistance/quro/core/QuroConversationPersistence.kt` — `loadAll()` 增加 `migrateAndClean()`：丢弃 `hidden`/空工具占位消息，清空垃圾工具结果内容（保留真实结果），空会话丢弃或占位。
  - `app/src/main/java/com/ai/assistance/quro/ui/QuroSoulUi.kt` — `AvatarContent` 兜底 `null/"?" -> "Q"`。
  - `app/src/main/java/com/ai/assistance/quro/ui/ChatScreen.kt` — `toPersona()` 的 `ava` 在 `name=="?"` 时回退 "Q"。
- 构建产物：`app/build/outputs/apk/debug/app-debug.apk`（已复制至 `C:/Users/admin/Desktop/QuroAI-debug.apk`）。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
