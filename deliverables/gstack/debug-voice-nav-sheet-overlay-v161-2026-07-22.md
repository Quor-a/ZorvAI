# 语音服务导航"进不去"根因排查与修复（v161）

**日期**：2026-07-22
**场景**：调试复盘
**参与成员**：排障手（gstack-investigator）

---

## 📌 TL;DR（执行摘要）
- 整体结论：🟡 有条件通过（待用户装 v161 复测确认）
- 阻塞项数量：0（代码层已修复，待验收）
- 下一步：用户装 `QuroAI-debug-2026-07-22-v161.apk`，确认「应用信息」= **1.0.161**，复测导航。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟡 条件 Go（代码已修，待真机验收） |
| 严重度分布 | 🔴 0 / 🟠 1（导航遮挡，非崩溃） / 🟡 0 / 🟢 0 |
| 关键行动项 | 1 条（用户复测 v161） |
| 建议负责人 | 排障手 |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- **核心判断**：`onOpenCloudConfig` 的 Toast 在真机弹了、但"面板已渲染"未弹，说明 `showVoiceService=true` 已执行、状态没问题；"进不去"是**视觉/层级问题**——进入 Hub 时底层设置弹层（`sheet`）未关闭，且 Hub 本身是 `Surface(BottomCenter, max 480dp)` 的**底部小弹层**，与底层设置弹层（含 `visible=sheet!=null` 遮罩、`settingsChildOpen` 路由）叠在一起互相干扰，用户在视觉上分不清"进没进"。
- **关键建议**：进入 Hub 时 `sheet=null` 关掉底层设置弹层，并把 Hub 从底部弹层改为**全屏覆盖**，与模型配置屏一致，彻底消除遮挡与约束歧义。

---

## 2. 综合审查发现

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟠 | 导航层级 | `ChatScreen.kt:959` / `:825` / `:982` | 进入语音服务 Hub 时未关闭底层设置弹层 `sheet`，且 Hub 为底部小弹层（`Surface(align BottomCenter, max 480dp)`），与底层 sheet 遮罩/内容叠加，造成"进不去"假象 | 进入时 `sheet=null`；Hub 改全屏覆盖 | 排障手 |

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 用户装 v161 复测：进 TTS 页点「前往语音服务设置 ›」应见**全屏** Hub；设置弹层内「语音服务」亦应进全屏 Hub | 用户 + 排障手 | P0 | 装包后即时 |

---

## ⚠️ 待完善 / 已知局限

- 诊断 Toast（`DBG·*`）仍保留在 v161 中，用于复测定位；若 v161 验收通过，下一版应移除这些 Toast 还干净。
- C 盘当前 99% 占用（剩 ~1G），桌面累积多版旧 APK，建议清理以腾空间（旧包已在 `D:\QuroAI_old_apks_backup\` 有备份，桌面临时副本可删）。

---

## 📚 成员产出索引

- gstack-investigator（排障手）原始产出：基于 `ChatScreen.kt` 全量读码 + 用户真机 Toast 信号（`onOpenCloudConfig→showVoiceService=true` 弹、`面板已渲染` 未弹）推导，结论为"状态置位成功但视觉被设置弹层遮挡"，非组合未进入。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
