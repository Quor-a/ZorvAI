# 情绪标签多标签连写解析 Bug 修复（v348）

**日期**：2026-07-27
**场景**：调试复盘（情绪/风格标签解析）
**参与成员**：排障手（调试与根因）

---

## 📌 TL;DR（执行摘要，3-5 行）
- 整体结论：🟢 通过（已修复并通过构建验证）
- 阻塞项数量：0
- 根因：`QuroVoiceStyle.kt` 的 `MARKER_RE` 仅匹配「行首一个括号」，导致 `(唱歌)(东北话)` 只提取第一个标签，第二个标签被当作字面文本遗留/错位。
- 修复：改为**循环剥离行首连续 `(标签)` 标记**，同时支持 `(a)(b)` 多括号连写 与 `(a b)` 单括号多标签 两种写法。
- 已构建 v348 APK（`QuroAI-debug-2026-07-27-v348.apk`）并通过 `cmp` 校验。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 1 / 🟢 0 |
| 关键行动项 | 3 条 |
| 建议负责人 | 排障手 |

---

## 1. 各成员核心结论（每位 1 段，别整段复制成员原文）

### 🔧 排障手（调试与根因）
- 核心判断：根因在 `QuroVoiceStyle.kt` 旧 `MARKER_RE = ^\(([^()]*?)\)\s*(.*)$` 只捕获行首「一个」括号——对 `(唱歌)(东北话)text`，仅 `group1=唱歌`、`group2=(东北话)text`，第二个标签沦为字面文本，最终被念成/显示为 `东北话()` 或被 MiMo 二次解析错位。
- 关键建议：用循环剥离行首连续 `(标签)` 标记（新增 `ONE_MARKER_RE` + `extractLeadingMarkers`），同时支持 `(a)(b)` 与 `(a b)`；`strip`/`segment`/`hasMarkers` 全部改用新逻辑。已用 Python 等价正则预演 8 个用例（含未闭合括号容错）全部通过；Gradle 构建 `BUILD SUCCESSFUL`。

> 仅排障手上场，其余成员未参与。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟡 | 功能 | `QuroVoiceStyle.kt:21`（旧 `MARKER_RE`） | 多标签连写 `(a)(b)` 仅解析第一个标签，后续标签丢失/被当字面文本 | 循环剥离行首连续括号标记 | 排障手 |

---

## 交付清单（代码变更 + 测试覆盖 + 发布检查清单 + 回滚预案）

- **代码变更**：
  - `QuroVoiceStyle.kt`：删除 `MARKER_RE`；新增 `ONE_MARKER_RE = ^\(([^()]*?)\)\s*` 与 `extractLeadingMarkers(line)`（循环剥离行首连续括号，返回 `(tags, 剩余文本)`）；重写 `segment` / `hasMarkers` / `strip` 全部走新逻辑；更新头部注释补充 `(a)(b)` 连写示例。
  - `app/build.gradle.kts`：`versionCode 347 → 348`、`versionName "1.0.347" → "1.0.348"`。
- **测试覆盖**：Python 等价正则预演 8 用例（单标签 / 单括号多标签 / 多括号连写 / 混合 / 普通行 / 未闭合括号 / 双空格分隔 / 用户原样串）全部符合预期；MiMo 消费端 `QuroTtsClients.kt:439` 会从解析出的 `[唱歌, 东北话]` 重建 `(唱歌 东北话) text` 单括号，无需改。
- **发布检查清单**：✅ 编译通过（仅历史 deprecation 警告，无错误）｜✅ APK 落桌面 `QuroAI-debug-2026-07-27-v348.apk` 并 `cmp` 一致（CMP_OK）｜✅ 版本号自增。
- **回滚预案**：git 回退 `QuroVoiceStyle.kt` 与 `app/build.gradle.kts` 两个文件至 v347 提交即可完整回滚。

---

## ✅ 行动清单（至少 3 条具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 真机验证 MiMo 下 `(唱歌)(东北话) 文本` 是否同时带「唱歌 + 东北话」音色（确认两个标签都生效） | 用户/QA | P1 | 装包后 |
| 2 | 顺带检查流式合成路径 `mimoStream`（`QuroTtsClients.kt:502` 直接 `strip` 丢弃标签）是否也需支持多标签逐段注入 | 排障手 | P2 | 下个迭代 |
| 3 | 在 `systemHint` 提示词示例中显式给出 `(a)(b)` 连写样例，引导 LLM 两种写法都可用 | 排障手 | P3 | 下个迭代 |

---

## ⚠️ 待完善 / 已知局限

- 流式合成（`mimoStream`）仍直接 `strip` 文本、不逐段注入情绪标签，多标签连写修复**仅对非流式路径生效**。
- 非 MiMo 云服务商走自然语言提示（`systemHintNatural`），本就不输出括号，多标签解析对其无影响（仅 display `strip` 更干净）。
- 未闭合括号（如 `(唱歌`）容错为字面文本保留在原文，符合预期，不吞片段。

---

## 📚 成员产出索引

- 排障手（排障手）原始产出：本会话内主理人直接执行——根因定位（`MARKER_RE` 单标签缺陷）+ 修复（`ONE_MARKER_RE` + `extractLeadingMarkers`）+ Python 等价正则预演 8 用例 + Gradle 构建与 APK 交付，详见本报告正文。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
