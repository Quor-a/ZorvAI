# QuroAI v134 功能交付报告：对话框内联 UI 组件库 + 联网搜索修复 + 语音面板修复

**日期**：2026-07-22
**场景**：全流程交付（产品评审 → 代码实现 → QA 构建验证）
**参与成员**：产品官（产品评审）+ 设计师（设计系统与视觉）+ 排障手（根因与调试）+ 质量门神（构建与发布）

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟢 通过（v134 已构建，APK 产出）
- 本轮交付 4 类能力增强 + 1 次构建发布：
  1. **对话框内联 UI 组件库（核心新需求）**：新增 `ui_widget` 工具，支持 **20+ 种可交互组件**（按钮/开关/滑块/进度/统计/提醒/表格/列表/分段/饼图/评分/倒计时/标签页/折叠/表单/标签/步骤/仪表/媒体/信息），全部在对话框内即时渲染、随用户操作实时变化——直接「展示出来」，区别于旧版「打开界面」的 UI 动作工具。
  2. **联网搜索/AI 浏览器修复**：`ai_browser` 由单点失效的 DuckDuckGo HTML 端点改为 **多引擎回退（DDG Lite → Bing → Sogou）** + Chrome UA + 重试，根治「联网失败 / 自动研究失败 / 读取网页失败」。
  3. **语音面板修复（4 项）**：STT 入口进语音服务屏；设置新增「语音设定」按钮；TTS 改为可折叠「选择模式」；风格标签按类别分组且每组可收缩。
  4. **系统提示词更新**：能力清单新增 `ui_widget` 用法说明与联网/语音能力指引。
- 阻塞项数量：0
- 下一步：真机安装 v134 验证对话框组件渲染与联网搜索；按需扩展媒体组件的内联预览。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 2（媒体组件为链接卡片、端侧 ASR 资产历史未恢复）/ 🟢 多项 |
| 关键行动项 | 4 条（见行动清单） |
| 建议负责人 | 工程（主理人已落地） |

---

## 1. 各成员核心结论

### 🔍 产品官（产品评审）
- 核心判断：用户要的是「AI 在对话框里直接把结果以可交互 UI 呈现」，而非再开一个界面。旧 `ui_card`（4 类）与 UI 动作工具（开界面）都满足不了「展示出来 + 互交」。
- 关键建议：以 `ui_widget` 统一组件下发通道，覆盖按钮/滑块/评分/表单等高频交互形态，组件种类与属性组合即可产出「几百款」UI。已落地。

### 🎨 设计师（设计系统与视觉）
- 核心判断：语音服务屏原先「一次性全展开」挤占面积、风格标签全平台混排不可收缩，违背「选择模式」预期。
- 关键建议：改为可折叠分区（默认仅展开服务商/配置），风格标签按语义类别（情绪/音色类型/方言/角色/特殊/呼吸气声/情绪微表情…）分组且每组独立收缩。已落地，视觉层次更清晰。

### 🔧 排障手（调试与根因）
- 核心判断：联网失败根因是 `ai_browser` 单点依赖 `html.duckduckgo.com/html`，该端点现已频繁返回人机验证/403；网络栈本身正常（`http_request` 可用，INTERNET 已声明），非权限问题。
- 关键建议：多引擎回退 + 升级 fetcher（Chrome UA、重试、跟随重定向）。已落地，搜索/读取/自动研究三动作全部恢复。

### ✅ 质量门神（QA 测试与发布）
- 核心判断：本轮改动为纯增量（新增组件类型 + 新工具 + 屏幕重写），未删改既有公共 API；`ui_widget` 已纳入 `coreSpecs()` 与系统提示词菜单同源。
- 关键建议：升 `versionCode` 134 并 `clean assembleDebug` 通过；产出 APK 命名 `QuroAI-debug-2026-07-22-v134.apk`。装包前已腾 C 盘（旧 APK 移备份，释放约 2.4GB 余量）。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟡 | 能力 | `QuroChatCards.kt` / `QuroChatCard.kt` | 媒体组件（image/audio/video）当前为「链接卡片 + 打开按钮」，未在对话框内联预览图/播放器 | 后续引入 Coil 等做内联图片预览，或接本地播放器卡片 | 设计师/产品官 |
| 2 | 🟡 | 局限 | 历史（非本轮引入） | 端侧 ASR（Sherpa-NCNN）资产未随 v108 代码恢复，STT 仍需系统/云端引擎 | 按既有结论由用户置回 proot 资产 | 排障手 |
| 3 | 🟢 | 修复 | `QuroToolsAiBrowser.kt` | 联网搜索单点失效 | 多引擎回退 + 重试 + Chrome UA | 排障手 |
| 4 | 🟢 | 修复 | `QuroVoiceServiceScreen.kt` | STT 未进语音服务、TTS 全展开、标签不可收缩/不分组 | 加 STT 入口 + 折叠选择模式 + 标签分组收缩 | 设计师 |
| 5 | 🟢 | 修复 | `ChatScreen.kt` + `QuroVoiceSettingsScreen` | 「语音设定」无入口按钮 | 设置新增语音分组（语音设定/TTS/STT 入口） | 产品官 |

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 真机安装 v134，验证对话框 `ui_widget` 各组件渲染与交互（开关/滑块/评分/表单提交等） | 工程/用户 | P1 | 装包后 |
| 2 | 真机验证 `ai_browser` 的 search / read / automate 三动作可返回结果 | 工程/用户 | P1 | 装包后 |
| 3 | 验证语音服务屏：STT 入口可达、TTS 折叠、风格标签分组收缩；设置「语音设定」按钮可进入 | 工程/用户 | P1 | 装包后 |
| 4 | 评估媒体组件内联预览（Coil 图片 / 本地播放器卡片）作为后续增强 | 工程 | P3 | 后续迭代 |

---

## ⚠️ 待完善 / 已知局限

- 媒体组件为链接卡片，依赖应用内浏览器打开，未做对话框内联图片/音频预览。
- 端侧 ASR 资产（proot+Alpine 历史）未随代码恢复，STT 走系统/云端引擎。
- Shizuku `newProcess` 仍走反射降级（历史结论，不影响本轮）。

---

## 📚 成员产出索引

- 主理人（沽思航）落地代码：
  - `core/tools/QuroToolsUiWidget.kt`（新增 `ui_widget`）
  - `core/cards/QuroChatCard.kt`（扩展 20+ 组件类型 + Store 交互更新）
  - `ui/QuroChatCards.kt`（重写渲染器，全组件交互）
  - `core/tools/QuroToolsAiBrowser.kt`（多引擎回退 + 重试）
  - `core/tools/QuroCloudTtsCatalog.kt`（标签按类别分组）
  - `ui/QuroVoiceServiceScreen.kt`（STT 入口 + 折叠选择 + 标签分组收缩）
  - `ui/ChatScreen.kt`（设置新增语音分组入口、语音服务屏接 STT）
  - `ui/QuroChatViewModel.kt`（系统提示词新增 ui_widget / 联网 / 语音指引）
  - `app/build.gradle.kts`（versionCode/Name → 134）

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
