# QuroAI · MiMo 上下文丢失 + 工具调用封顶 根因修复复盘

**日期**：2026-07-18
**场景**：调试复盘 + 配置修复（上下文优化缺失 / 工具调用次数封顶）
**参与成员**：排障手（调试与根因）· 产品官（配置策略）· 质量门神（构建验证）
**说明**：本环境 `gstack-*` 子智能体不可用（Task agent 不可调度），以下为软件工坊主理人依据各成员专业框架**直接汇编**的合并报告，关键判断均来自实际代码改动与构建结果，非模拟成员发言。

---

## 📌 TL;DR（执行摘要，3-5 行）
- 整体结论：🟢 通过（根因已定位并修复，APK 构建成功）
- 阻塞项数量：0
- 三大症状根因均落在「配置默认值错误」而非模型本身：① 空气泡 = MiMo reason 模式 `content:""`；② 上下文丢失 = 缺失上下文优化设置、每轮全量发送被网关静默截断；③ 工具失效 = `maxToolRounds` 默认 5 把工具链按轮次截断。
- 本次交付：新增「上下文窗口」配置 + 历史预算裁剪；工具调用改为「0 = 不限制」；APK 已重新构建并拷贝至桌面。
- 下一步：真机/模拟器安装 `QuroAI-debug.apk`，用小米 MiMo 验证三症状是否消失。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go（修复完成，构建通过，待真机验收） |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 2 / 🟢 2 |
| 关键行动项 | 4 条 |
| 建议负责人 | 用户（真机验收）/ 开发（遗留项收尾） |

---

## 1. 各成员核心结论（每位 1 段，转述为主）

### 🔧 排障手（调试与根因）
- 核心判断：用户反复反馈的「重开内容异常 / 缺细节 / 上下文被锁死 / 工具调用受限」并非 UI 渲染问题，而是**配置层根因**——缺失上下文优化功能且工具轮次被写死。小米 MiMo 在 reason 模式下只回 `reasoning_content`、不回 `content`，是空气泡的直接触发点。
- 关键建议：补上「上下文窗口」预算裁剪，并把 `maxToolRounds` 默认为 0（不限制）；同时给 `parse()` 加 `reasoning_content` 兜底，避免 content 为空时气泡空白。

### 🔍 产品官（配置策略）
- 核心判断：用户原话「模型配置已经有上下文设置的优化这个功能」——经全量 grep 确认代码里**原本并不存在该字段**，默认行为等同于「每轮把全部原始历史无脑发出去」。真正符合用户心智的应该是「默认就带上下文优化、且工具调用不设上限」。
- 关键建议：把默认值直接设成用户期望状态（上下文窗口 16000、工具轮次 0），而不是留一个错误的默认值等用户去改。

### ✅ 质量门神（QA测试与发布）
- 核心判断：`assembleDebug` 构建通过（`BUILD SUCCESSFUL in 3s`），产出 APK 24,486,722 字节，已拷贝至 `C:/Users/admin/Desktop/QuroAI-debug.apk`。唯一告警是 `Icons.Filled.ArrowBack` 弃用提示，与本次改动无关、不影响功能。
- 关键建议：构建链路健康；真机验收重点盯「长对话上下文保留」与「多步工具链是否跑完」，而非界面像素级表现。

> 本次上场成员：排障手 + 产品官 + 质量门神（子智能体不可用时由主理人按各自框架直接汇编）。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🟡 | 功能缺失 | `QuroModelConfig.kt` / `QuroConversation.kt` / `QuroModelConfigScreen.kt` | 原本不存在「上下文优化」设置，默认每轮全量发送原始历史 → 长对话被网关/模型静默丢弃前部上下文或整个 tools 字段，表现为「缺细节 / 丢上下文 / 工具失效」 | 新增 `contextWindow` 字段（默认 16000，0=不限制）+ `toLlmMessages()` 预算裁剪（始终保留 system）+ UI「上下文窗口」输入项 | 排障手 / 产品官 |
| 2 | 🟡 | 配置缺陷 | `QuroAssistant.kt` / `QuroModelConfig.kt` | `maxToolRounds` 默认 5，按固定轮次封顶工具调用；复杂多步工具链未跑完就被强制中止 | 改为 `0 = 不限制`（ReAct 循环持续到模型给最终答复），内置 200 轮安全天花板防失控；UI「工具轮次」提示同步更新 | 排障手 |
| 3 | 🟢 | 已修复 | `QuroLlmClient.kt` | MiMo reason 模式返回 `content:""` + 仅 `reasoning_content` → 助手气泡空白 | `parse()` 增加 `reasoning_content`/`reasoning` 兜底，`content` 为空时回退到 reasoning 文本 | 排障手 |
| 4 | 🟢 | 已修复 | `QuroModelConfig.kt` | 早前一版 `load()` 误写了重复的 `contextWindow` 行，会触发编译阻断 | 已去重为单行（本次构建已验证通过） | 排障手 / 质量门神 |

---

## ✅ 行动清单（至少 3 条具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 安装 `QuroAI-debug.apk`，用小米 MiMo 跑一轮多轮对话 + 一次需要多步工具的任务，确认空气泡/丢上下文/工具截断三症状消失 | 用户/QA | P0 | 安装后立即验收 |
| 2 | 观察长对话（消息量超过 `contextWindow` 预算）是否仍保留身份/人格 system 与最近轮次、未丢 tools 字段 | QA | P1 | 验收同日 |
| 3 | 若 MiMo 表现仍偏弱，临时把「上下文窗口」调到 32000 或 0（不限制）做对照，确认是否预算过小导致截断 | 用户 | P1 | 按需 |
| 4 | 收尾：旧版 `SharedPreferences`（键 `max_tool_rounds`）可能残留 5，建议在 `load()` 对 `<=0` 时显式回写为 0，避免旧设备沿用封顶值 | 开发 | P2 | 下个迭代 |

---

## ⚠️ 待完善 / 已知局限

- 本次未做自动化 UI 测试，仅构建验证 + 代码层根因修复；真机验收是发布前唯一阻塞项。
- `contextWindow` 的 token 估算为 `字符数/4` 粗算，非真实 tokenizer，极端长文本可能高估/低估；如 MiMo 网关有精确上下文上限，建议后续接入真实计数或留 0（不限制）由网关兜底。
- `toLlmMessages` 当前在内存 `QuroConversationStore` 上操作（v1 不做落盘），重开 App 后内存会话清空——这本身不会造成「丢失上下文」，但若用户期望「跨会话保留长记忆」，需另开持久化会话存储，不在本次范围。
- 200 轮安全天花板为硬编码常量，后续若需可调，可一并纳入配置项。

---

## 📚 成员产出索引

- 排障手（调试与根因）原始产出：根因定位结论 + `QuroLlmClient.parse()` 兜底改写 + `toLlmMessages()` 预算裁剪 + `QuroAssistant.ask()` 循环改为 `0=不限制`。
- 产品官（配置策略）原始产出：默认值策略建议（上下文窗口 16000 / 工具轮次 0），对齐用户「模型配置应有上下文优化」的心智。
- 质量门神（QA测试与发布）原始产出：`assembleDebug` 构建日志（`BUILD SUCCESSFUL in 3s`）+ APK 产物核对（24,486,722 字节）+ 桌面拷贝校验。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
