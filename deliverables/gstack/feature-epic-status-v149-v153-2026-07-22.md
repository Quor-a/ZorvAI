# QuroAI 功能史诗状态报告（v149 → v153）

**日期**：2026-07-22
**场景**：调试复盘 + 全流程交付（导航 bug 根因 + 气泡任务栏/询问 交付）
**参与成员**：排障手（根因审计）、质量门神（发布门禁）、主理人（直接实现，本环境 gstack 子代理不可用，依既定规则主理人直干）

---

## 📌 TL;DR（执行摘要）

- **整体结论：🟡 条件 Go** — v152 已修复「前往语音服务设置进不去」导航 bug 并出包；v153（构建中）新增聊天气泡任务栏（复制/追问/分享/重试）。v149/v150「组件合体气泡 / 元宝卡片」经审计**代码完整且在位**，此前"看不到"是陈旧/损坏安装包所致。
- **MCP 多服务端已支持**（列表化存储 + 保存继续添加），"只能加一个"为陈旧安装假象。
- **阻塞项**：① v149/v150 仍需真机 clean reinstall 确认；② MCP「部署到终端 + 实时更新 + 内置若干」尚未实现（本轮排期下一轮）。
- **下一步**：装 v153 复测气泡任务栏/追问；随后实现 MCP 部署到终端。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟡 条件 Go（clean reinstall 真机验证后转 Go） |
| 严重度分布 | 🔴0 / 🟠0 / 🟡1（陈旧安装，非缺陷）/ 🟢 多项已落地 |
| 关键行动项 | 4 条（见下） |
| 建议负责人 | 主理人（Gu）/ 用户真机验证 |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- **「前往语音服务设置」进不去**：根因为 v146 宣称的"嵌入式导航"（`embeddedVoiceService`）从未真正写入文件，代码仍走跨 overlay 的 `showTts=false; showVoiceService=true` 切换，Compose 重组时序竞争吞掉点击。v152 改为在 `QuroTtsSettingsScreen` 内部 `embeddedVoiceService` 状态切换，零跨 overlay。
- **v149/v150 不可见**：独立审计 `ChatScreen.kt`（气泡合体 `msg.cards+inlineCards`、正则 `yb/yuanbao.tencent.com`）、`QuroChatCard.kt`（YuanbaoCard + 36+ 组件）、`QuroChatCards.kt`（派发+渲染+点击内置浏览器）全部到位，**非代码缺失**。
- **MCP 多服务端**：`QuroMcpClientPrefs` 为 JSONArray 列表（`load/add/save` 均追加语义），设置页「保存并继续添加」清空表单保留列表——**已支持多服务端**。

### ✅ 质量门神（QA 与发布）
- **v152 发布门禁**：🟡 条件 Go。APK 194,900,885 字节（与构建产物 cmp 一致，非截断）；C 盘余 1.8GB（≥500MB 达标但偏紧）。
- **验证清单**：卸载旧包 → 装 v152/v153 → ① 设置页「后台自启动」tab ② 发 `yb.tencent.com` 链接看元宝卡 ③ 让 AI 下发组件看合体 ④ 系统分享拉起 ⑤ 重启看自启语音球。任一 FAIL → No-Go。
- **回滚**：异常则 `adb uninstall` 后装回 `D:\QuroAI_old_apks_backup\` 备份包（先确认 C 盘余量）。

### 🧑‍💻 主理人（直接实现）
- **v153 交付**：聊天气泡任务栏（`MessageRow` 内，仅 AI 消息，纯文字按钮零资源风险）：复制 / 追问（预填输入框引用上文）/ 分享（系统分享）/ 重试（重发上一条用户消息）。回调 `onAskFollowup/onShare/onRegenerate` 从主作用域经 `MessageList` 穿到 `MessageRow`。

---

## 2. 综合审查发现（按严重度）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源 |
|---|--------|------|------|---------|------|------|
| 1 | 🟡 | 导航 | QuroTtsSettingsScreen.kt | v146 嵌入式导航未落地，跨 overlay 切换吞点击 | v152 已改为页内 `embeddedVoiceService` 切换 | 排障手 |
| 2 | 🟡 | 安装 | APK 安装 | 用户长期测到陈旧/损坏包，误判功能缺失 | clean reinstall 最新版 | 质量门神 |
| 3 | 🟢 | 组件 | ChatScreen/QuroChatCards | v149/v150 代码完整在位 | 无需改，真机复测 | 排障手 |
| 4 | 🟢 | MCP | QuroMcpClientPrefs | 多服务端已支持 | 无需改 | 排障手 |
| 5 | 🟢 | 气泡 | ChatScreen.kt MessageRow | v153 新增任务栏+追问/分享/重试 | 已落地，真机验证 | 主理人 |

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 卸载旧包，装 `QuroAI-debug-2026-07-22-v152.apk`（或待 v153 出包后装 v153） | 用户 | P0 | 立即 |
| 2 | 真机按触发条件复测 v149（AI 下发组件）/v150（元宝链接）/v151（自启/通知栏/分享） | 用户 | P0 | 装后 |
| 3 | v153 验证气泡任务栏：AI 消息下应见 复制/追问/分享/重试；点追问输入框预填引用 | 用户 | P1 | 装后 |
| 4 | 实现 MCP「部署到终端 + 实时更新 + 内置若干」：下一轮，先对齐终端语义与内置清单 | 主理人 | P1 | 后续 |

---

## ⚠️ 待完善 / 已知局限

- **MCP 部署到终端尚未实现**：用户要求"部署 MCP 技能到终端、MCP 实时更新到软件供 AI 使用、内置几个部署到终端"。本轮未做，需读取 `QuroTerminalController`（v127 重构后的应用内 shell）API 后设计：内置 MCP 清单 → 写启动脚本 → 终端拉起 → 注册进 `QuroMcpClientPrefs` 使 AI 经 `mcp_call` 即时可用。
- 气泡"重试"为重发上一条用户消息，不保证模型给出不同答案（取决于模型）。
- 组件/卡片为"AI 下发才出现"，非常驻，复测须走真实 AI 对话。

---

## 📚 成员产出索引

- 排障手根因审计：见 `deliverables/gstack/feature-v151-ship-v149-v150-audit-2026-07-22.md`
- 质量门神发布门禁：同上报告（Release Gate 章节）
- 主理人实现：ChatScreen.kt（MessageRow 任务栏 + 回调链）、QuroTtsSettingsScreen.kt（v152 嵌入式导航）、build.gradle.kts（v152/v153）

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
