package com.ai.assistance.quro.genui.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * 打字机美学 —— 只有"壳"用这套；
 * WebView 里的世界是 AI 自由发挥的（端上零样式约束）。
 */
object GenTheme {
    val Screen   = Color(0xFF14110E)   // 暖黑屏底
    val Panel    = Color(0xFF1D1915)
    val PanelUp  = Color(0xFF241F19)
    val Line     = Color(0xFF2E2820)
    val Amber    = Color(0xFFE29A45)   // 打字机色带
    val AmberDim = Color(0xFF8A6535)
    val Paper    = Color(0xFFF0E9DC)   // 稿纸（WebView 区底色）
    val Text     = Color(0xFFEDE6D9)
    val Dim      = Color(0xFF7A6F60)
    val Green    = Color(0xFF6F9B5E)
    val Red      = Color(0xFFC45A4A)
}
