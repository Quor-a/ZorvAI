# TypeScript 项目

- `src/index.ts` —— 入口
- `src/utils.ts` —— 模块（演示 import/export + 接口）
- 运行时内置 `types/quro.d.ts`（Tools / _ / dataUtils 全局类型声明）

ZorvAI 运行时自动把 TS 转译成 CommonJS JS 执行：
code_runner(path="项目名/src/index.ts")。
