# Android 项目

最小 Activity 工程。可配合 ZorvAI 构建台（ACI build_apk）在设备内编译成 APK。

- `app/src/main/java/com/example/myapp/MainActivity.java` —— 入口 Activity
- `app/src/main/AndroidManifest.xml` —— 清单
- `app/build.gradle` —— 构建脚本
