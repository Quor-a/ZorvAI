# Built application files
*.apk
# NOTE: 保留端侧 APK 构建器的模板 APK（位于 assets 内，是构建引擎注入 DEX 的底座，非构建产物）
!app/src/main/assets/libs/common/base.apk
*.aar
*.ap_
*.aab

# Files for the ART/Dalvik VM
*.dex

# Java class files
*.class

# Generated files
bin/
gen/
out/
build/
# NOTE: 保留端侧 APK 构建器源码包（包名 com.ai.assistance.quro.build，目录名恰为 build，
# 与 Gradle 构建输出 build/ 同名，必须显式解除忽略，否则源码不会被提交）
!app/src/main/java/com/ai/assistance/quro/build/
release/

# Gradle files
.gradle/
gradle/wrapper/gradle-wrapper.jar

# Local configuration (SDK path, etc.)
local.properties
gradle.properties.local

# Log/Output files
*.log
build_*.log

# IDE / Editor
.idea/
*.iml
*.ipr
*.iws
.vscode/
.project
.classpath
.settings/
.DS_Store
Thumbs.db

# Kotlin / Compose
.kotlin/
buildTmp/

# Native / NDK
.externalNativeBuild/
.cxx/
*.so.tmp

# OS / misc
nul
*.tmp

# Dev scratch / temp
_novaterm_temp/
measure_sysprompt.py

# Local gradle cache mirror (never commit)
gradle_cache/

# Keystore / signing files (never commit)
*.jks
*.keystore
*.p12
*keystore*
# 注意：keystore.properties 包含密码，不应提交到 git
# 但在本地开发时需要保留此文件
keystore.properties

# Backup / outputs (large files, never commit)
.backup/
outputs/
*.backup
*.bak*
*.bak2
*.orig
*.swp
*.swo
*~
.git-rewrite/

# Large model files
*.onnx
*.tar.gz
*.tar.xz

# Dev test / scratch files
test_*.py
test_*.html
repro_roundtrip.py
audit_tools.py
release-apk-*.md
measure_sysprompt.py

# Internal deliverables / debug docs
deliverables/

# Test certificates
*.pem
*.pk8

# Keystore variants
zorvai_release.jks
debug-for-release.keystore

# ===== ZorvAI 签名安全加固（scripts/sign.sh 配套）=====
# 上游已有 *.jks / *.keystore / keystore.properties，此处补齐边界情况

# 私钥与凭证（任何形态）
*.keystore.*
keystore.properties.*
.env
.env.*
env.sh
secrets/
.secrets/

# 签名产物（绝不入库）
*-signed.apk
*-unsigned.apk
*-aligned.apk
build/outputs/apk/

# 本地签名环境（若有人误把密钥位放到项目内）
.zorvai-signing/
signing/

# 例外：签名工具脚本本身不含私钥，必须可入库
# （否则会被上面的 *keystore* 通配规则误伤）
!scripts/setup-keystore.sh
!scripts/lib/common.sh
