org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=false
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableResourceOptimizations=false
android.enableArtProfile=false
android.experimental.enableArtProfile=false
android.versionControlInfoEnabled=false
kotlin.code.style=official

# GitHub 登录（PAT 或 OAuth 设备流；均为 public client，无需 Client Secret）：
# 下面填 GitHub OAuth App 的 Client ID（公开标识，可安全写入包内）。设备流无需注册回调地址。
# Client Secret 不要写进仓库/APK——设备流与 PAT 都不需要它，写进 APK 反而会被反编译泄露。
GITHUB_CLIENT_ID=Ov23lihueyt2c8QhHWxO
