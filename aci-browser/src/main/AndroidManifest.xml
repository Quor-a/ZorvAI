<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ACI 协议权限定义：受控端必须声明，否则 Service 上的 android:permission 指向不存在权限 → 绑定失败 -->
    <permission android:name="ai.aci.permission.CALL" android:protectionLevel="normal" />
    <permission android:name="ai.aci.permission.CALL_DANGEROUS" android:protectionLevel="dangerous" />
    <permission android:name="ai.aci.permission.DISCOVER" android:protectionLevel="normal" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="ai.aci.permission.CALL" />
    <uses-permission android:name="ai.aci.permission.DISCOVER" />
    <uses-permission android:name="ai.aci.permission.CALL_DANGEROUS" />

    <!-- Android 11+ 包可见性：让控制方（ZorvAI）能发现并绑定本 App 的 ACI Service（手册第三节明确要求） -->
    <queries>
        <intent>
            <action android:name="ai.aci.core.ACTION_BIND" />
        </intent>
        <intent>
            <action android:name="ai.aci.core.ACTION_WAKE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:label="Zorv 受控浏览器"
        android:supportsRtl="true"
        android:theme="@style/Theme.Browser">

        <activity
            android:name=".BrowserActivity"
            android:exported="true"
            android:label="Zorv 受控浏览器"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 受控端 ACI Service：仅放行持有 ai.aci.permission.CALL 的调用方（ZorvAI 主程序） -->
        <service
            android:name=".QuroControlledAciService"
            android:exported="true"
            android:permission="ai.aci.permission.CALL">
            <intent-filter>
                <action android:name="ai.aci.core.ACTION_BIND" />
            </intent-filter>
        </service>

        <!-- 唤醒 Receiver：ZorvAI 在停止态绑定前先发广播拉起进程 -->
        <receiver
            android:name=".QuroAciWakeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="ai.aci.core.ACTION_WAKE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
