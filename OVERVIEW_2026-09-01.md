# 概览：对话框化小窗 + 文档排版内联预览 + 工具分类/系统提示/文档同步（#581–#585）

> 工程：`D:\Calw OS-project\QuroAI`（包名 `com.ai.assistance.quro`，版本 1.0.75/1075 未动）
> 续做指令④；前序已完成 #581 内置浏览器化小窗、#582 对话框化小窗、#580 browser_act 线程修复。

## 交付内容

### #583 对话框文档排版内联预览（本回合核心）
- 根因确认：`RichText` 已在 `ChatScreen:2911` 接线（MsgBlock.Text→RichText），`QuroDocumentViewer` 也已接线（附件点击全屏预览）。但气泡里只有 120 字纯文本片段，**没有在对话框内联渲染真实排版**。
- 修复：`AttachmentFilePreview` 为 docx/xlsx/pptx/pdf 新增「对话框内预览排版」展开按钮，复用 `QuroDocumentViewer`（docx→mammoth.js、xlsx→SheetJS、pdf→pdf.js）在气泡内 `heightIn(260..420).dp` 受限容器内联渲染真实版面；卡片点击仍走全屏查看器。

### #584 工具分类构架 + 系统提示
- `QuroBuiltInTools.kt` 顶部新增「能力域→代表工具」分类总览注释（16 个域，含化小窗标注为纯 UI）。
- `QuroChatViewModel.buildSystemPrompt` 末尾（近因偏好最高处）追加「应用内新增能力」速查段，列出 `browser_act`/`chat_doc`/`ai_browser`/`web_crawler`/`lsposed`/`priv_exec`/`adb_term`/`packet_capture`/化小窗，工具名经 grep `override val name` 核验一致。

### #585 文档同步
- `README.md` 新增「近期新增功能（v1.0.75）」小节 + 目录条目（仅 README 范畴，未触碰 ACI 手册）。

## 铁律遵守
包名/类名/Manifest/路径未动；版本号未动；未定义 `ai.aci.permission.*`；未破坏 pipe/shell；WebView 反射隐藏。

## 编译验证
`compileFullDebugKotlin` 两次均 **BUILD SUCCESSFUL**（仅既有 ClickableText deprecation 警告，非本次引入）。

## 改动文件
- `app/.../ui/ChatScreen.kt` — AttachmentFilePreview 内联文档排版预览
- `app/.../ui/QuroChatViewModel.kt` — 系统提示新增能力速查
- `app/.../core/tools/QuroBuiltInTools.kt` — 工具分类总览注释
- `README.md` — 近期新增功能小节
