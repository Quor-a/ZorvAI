# ZorvAI 权限与系统能力清单

> 适用应用：**ZorvAI（QuroAI）** · 包名 `com.ai.assistance.quro`
> 本文档逐项说明 ZorvAI 所需的 Android 权限与系统能力：**用途 / 授予方式 / 隐私边界**。
> 所有特权通道均**需用户显式授权**后才会启用；未授权时返回引导提示，不会静默越权（对应 README「Privilege Tiers 特权层 L1–L5」）。

---

## 一、界面叠加与浮窗

### 1. 自由浮窗（悬浮窗）
- **Android 权限**：`SYSTEM_ALERT_WINDOW`
- **用途**：在任意应用上层显示 AI 对话胶囊 / 工具箱浮窗 / 快捷操作层。
- **授予**：系统设置 → 应用 → 特殊应用权限 → 显示在其他应用上层。
- **隐私**：仅绘制 UI 层，不读取其他应用内容。

### 2. 发送全屏通知
- **Android 权限**：`POST_NOTIFICATIONS`（Android 13+）+ 全屏 Intent（`setFullScreenIntent`）
- **用途**：后台运行的应用可在闹钟 / 来电等场景弹出覆盖式新界面，显示在当前应用上方。
- **授予**：通知权限授权后，系统对全屏 Intent 按需放行（用户可在设置中关闭）。
- **隐私**：仅承载 ZorvAI 自身通知内容。

### 3. 画中画（PiP）
- **能力**：`PictureInPicture`（`android:supportsPictureInPicture`，非运行时弹窗）
- **用途**：离开应用（如继续观看视频）时，在其它应用上层保留小窗。
- **授予**：功能开关，无危险权限弹窗。
- **隐私**：仅渲染本应用内容。

---

## 二、输入与交互代理

### 4. 自动填充服务
- **Android 权限**：`android.permission.BIND_AUTOFILL_SERVICE`（manifest 声明 + 用户在设置中选中本服务）
- **用途**：作为系统自动填充服务，在其它 App 输入框提供 AI 生成的填充建议。
- **授予**：设置 → 密码与账号 → 自动填充服务 → 选择 Zorv AI。
- **隐私**：填充内容在端侧处理；建议仅对可信字段启用。

### 5. 内容拖拽（工具调用）
- **能力**：DragAndDrop 框架；跨应用拖拽可能涉及无障碍（`BIND_ACCESSIBILITY_SERVICE`）桥接。
- **用途**：AI 通过工具调用在应用间拖拽文本 / 图片 / 文件。
- **授予**：随工具调用在用户操作上下文中触发；跨应用无障碍桥接需无障碍授权（对应 L1）。
- **隐私**：仅传输用户主动拖拽的内容。

### 6. 截屏手势（AI 学习各品牌手势）
- **用途**：让 AI 了解各厂商的截屏手势（如三星掌心滑动、小米三指下拉、华为指关节双击等），以便自动触发或在对话中引导用户。
- **实现**：以端侧知识库 / 语义识别为主；实际截屏可走 `MediaProjection` 或系统截屏 API。
- **授予**：如涉及屏幕捕获，需用户授权（同「屏幕录制」）。
- **隐私**：手势识别在端侧完成，不采集按键内容。

---

## 三、屏幕捕获

### 7. 屏幕录制（工具调用）
- **Android 权限**：`MediaProjection`（每次录制需用户即时同意）
- **用途**：AI 通过工具调用发起录屏，用于演示、故障诊断、向用户展示操作。
- **授予**：每次启动录屏弹系统确认框，用户可拒绝。
- **隐私**：录制内容存于本机；建议仅录制授权区域。

---

## 四、通知与提醒

### 8. 设备和应用通知
- **Android 权限**：`BIND_NOTIFICATION_LISTENER_SERVICE`（通知侦听）+ `POST_NOTIFICATIONS`
- **用途（用户允许后）**：
  - 读取所有通知（**包括联系人姓名和通知内容等个人信息**）；
  - 清除通知或触发通知中的按钮；
  - 开启或关闭勿扰模式；
  - 修改通知相关设置。
- **授予**：设置 → 通知 → 通知访问 → 启用 Zorv AI。
- **隐私**：⚠️ **高敏感**。读取内容仅用于本机 AI 理解上下文；建议在 App 内限定允许读取的应用，对含敏感信息的通知谨慎授权。

### 9. 闹钟和提醒
- **Android 权限**：`SCHEDULE_EXACT_ALARM` / `USE_EXACT_ALARM`（精确闹钟）
- **用途**：在精准时间触发闹钟提醒并执行相关操作（定时任务、提醒等）。
- **授予**：`SCHEDULE_EXACT_ALARM` 需在设置中授予（Android 12+）；`USE_EXACT_ALARM` 限闹钟类应用。
- **隐私**：仅创建本应用提醒。

### 10. 开启屏幕
- **Android 权限**：`WAKE_LOCK` + `FLAG_TURN_SCREEN_ON`（点亮屏幕，非独立危险权限）
- **用途**：闹钟 / 接收通知等场景触发时点亮屏幕。
- **授予**：随通知 / 闹钟能力启用。
- **隐私**：仅控制屏幕亮灭。

---

## 五、媒体与文件

### 11. 媒体管理应用
- **Android 权限**：媒体读写（`READ_MEDIA_IMAGES` / `READ_MEDIA_VIDEO` / `READ_MEDIA_AUDIO`）+ 「媒体管理」特殊权限（设置 → 特殊应用权限 → 媒体管理）
- **用途（允许后）**：可直接修改或删除其它应用创建的媒体文件，**无需事先征得同意**。应用必须具备访问文件和媒体的权限。
- **授予**：设置 → 特殊应用权限 → 媒体管理 → 授予 Zorv AI；并要求已有文件与媒体访问权限。
- **隐私**：⚠️ **高敏感**。请仅对可信场景授予；App 内提供操作确认，避免误删。

---

## 六、本机访问与工具集成

### 12. 网页端访问本机（集成在工具箱，注册成工具）
- **能力**：在设备启动轻量 LAN Web 服务，将本机能力暴露给同网网页端（**各品牌手机适配**）。
- **用途**：在电脑 / 平板的浏览器中访问并操控本机 AI 与文件。
- **授予**：随工具箱「网页访问本机」工具启用；建议仅在可信 Wi-Fi 下开启。
- **隐私**：仅暴露用户启用的本机能力；建议设置访问口令，注意局域网安全。

---

## 七、平台依赖

### 13. Google 服务（GMS）
- **说明**：部分能力依赖 Google Mobile Services（推送、地图、部分账号体系等）。
- **影响**：无 GMS 设备（部分国产 ROM）相关功能降级，**核心端侧能力不受影响**。

### 14. Android System WebView
- **说明**：系统 WebView 用于渲染网页、文档预览、受控浏览器等。
- **影响**：需系统 WebView 可用；部分 ROM 可替换为系统浏览器引擎。

---

## 八、开发者选项 / 实验特性

### 15. 多窗口显示不可调整大小的应用
- **设置项**：开发者选项 → 「强制以多窗口模式显示不可调整大小的应用」
- **用途**：让本不支持分屏的应用也能多窗口运行，提升工具箱 / 浮窗协同体验。
- **授予**：ZorvAI 请求在开发者选项中启用该项（需开启开发者模式 / USB 调试）。

### 16. 强制桌面模式（辅助显示屏，实验性）
- **设置项**：开发者选项 → 「强制桌面模式」（辅助显示屏）
- **用途**：在外接显示器上以桌面模式呈现，获得更大的多窗口工作区。
- **授予**：实验性功能，需在开发者选项中启用。

### 17. 覆盖「强制启用 SmartDark」
- **设置项**：开发者选项 → 「强制启用 SmartDark」/「始终开启 SmartDark」
- **用途**：ZorvAI 可覆盖该设置，统一深色渲染策略。
- **授予**：需在开发者选项中允许覆盖。

---

## 九、调试通道

### 18. USB 授权 / 无线调试（本应用直接授权调试）
- **能力**：ADB 调试授权（USB 调试 + 无线调试）；ZorvAI 可在 **UI-工具箱内直接授权调试**（结合 Shizuku / 自身 ADB 桥）。
- **用途**：为端侧开发、工具注入、Shizuku 配对提供调试通道。
- **授予**：设置 → 系统 → 开发者选项 → USB 调试 / 无线调试；授权弹窗由本应用在工具箱内引导完成。
- **隐私**：⚠️ 调试通道权限高，仅在可信环境启用；可在工具箱内随时撤销。

---

## 隐私与授权总原则

- **最小授权**：每项特权均**需用户显式开启**，未授权不会静默越权（对应 README 特权层 L1–L5）。
- **敏感项提示**：通知读取（#8）、媒体管理（#11）、调试通道（#18）为高敏感项，App 内均给出用途说明与撤销入口。
- **端侧优先**：能力默认在设备本地执行；授权范围由用户在系统设置与 App 内共同控制。
- **随时可撤**：所有权限均可在系统设置或 App 内「权限管理 / 审计」中关闭或重置。
